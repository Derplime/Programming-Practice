import java.util.*;

public class bijele {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.println(
            (sc.nextInt() - 1)*-1 + " " +
            (sc.nextInt() - 1)*-1 + " " +
            (sc.nextInt() - 2)*-1 + " " +
            (sc.nextInt() - 2)*-1 + " " +
            (sc.nextInt() - 2)*-1 + " " +
            (sc.nextInt() - 8)*-1
        );
    }
}